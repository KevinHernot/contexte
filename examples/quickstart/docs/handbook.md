# Quickstart Handbook

Contexte turns messy documents into structured AI context.

## Usage

Run `ctx build ./docs --to docs.ctxpack --report` to create a portable pack.

## Provenance

Every chunk in the pack keeps a source reference.
