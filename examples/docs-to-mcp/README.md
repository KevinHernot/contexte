# docs-to-mcp

MCP serving is planned as an optional experimental read-only adapter after the v0.1 core matures. For v0.1, build a pack and use `contexte.mcp.tools.search_context` plus the other local read-only helpers.
